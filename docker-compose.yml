services:
  contentalchemy:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./config:/app/config
    command: streamlit run src/web_app/streamlit_app.py --server.port=8501 --server.address=0.0.0.0
