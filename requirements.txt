# Python 3.12
# Versions below are pinned to what milestone 1 was built and tested against
# (validated via `pytest`, 2025). Bump deliberately, not implicitly via >=.

# Core orchestration
langgraph==1.2.10
langchain==1.3.14
langchain-core==1.5.3

# LLM providers
openai==2.52.0
anthropic==0.120.2
langchain-openai==1.4.1
langchain-anthropic==1.5.3
google-generativeai==0.8.6
langchain-google-genai==4.3.2

# Research providers
google-search-results==2.4.2   # SerpApi client (imports as `serpapi`)
requests==2.34.2                # Perplexity Sonar (raw HTTP)

# Image generation
pillow==12.3.0

# Web interface
streamlit==1.60.0

# Config / session persistence
python-dotenv==1.2.2
PyYAML==6.0.3
langgraph-checkpoint-postgres==3.1.1   # primary session store (Supabase Postgres)
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
langgraph-checkpoint-sqlite==3.1.1     # alternative backend for fully-offline/no-external-DB use
aiosqlite==0.22.1

# Content quality & SEO utilities
textstat==0.7.13                # Flesch-Kincaid readability scoring

# Observability (optional — tracing dashboard for graph.invoke() runs)
langsmith==0.10.2   # verify against `pip index versions langsmith` before installing

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
